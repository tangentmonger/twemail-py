language: python
python:
    - "3.2"
# command to install dependencies
install: "pip install -r requirements.txt"
#command to run tests
script: nosetests
env:
    global:
        - consumer_key=""
        - consumer_secret=""
        - access_token_key=""
        - access_token_secret=""
        - local_timezone = ""
        - record_path = ""
        - email_address = ""
